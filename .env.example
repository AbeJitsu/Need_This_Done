# Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# Never commit .env with real secrets - this file (.env.example) is safe to share
#
# This file is for Docker Compose and infrastructure-level configuration.
# For Next.js application-level variables, see app/.env.example

# ============================================================================
# Medusa Ecommerce Backend Configuration
# ============================================================================
# Medusa is the headless commerce engine. These variables configure the
# PostgreSQL database and JWT authentication for the Medusa backend service.

# PostgreSQL password for Medusa database
# Generate a secure password: openssl rand -base64 32
MEDUSA_DB_PASSWORD=your-secure-db-password-here

# JWT Secret for Medusa user authentication
# Generate with: openssl rand -base64 32
# Used to sign authentication tokens for customer sessions
MEDUSA_JWT_SECRET=your-medusa-jwt-secret-here

# JWT Secret for Medusa admin authentication
# Generate with: openssl rand -base64 32
# Used to sign tokens for admin dashboard access
MEDUSA_ADMIN_JWT_SECRET=your-medusa-admin-jwt-secret-here

# ============================================================================
# Supabase Configuration - Database & Authentication
# ============================================================================
# Supabase provides PostgreSQL database, real-time subscriptions, and auth.
# Get these from: https://app.supabase.com → Project Settings → API
#
# The public key (anon key) is safe to expose - it's protected by Row Level Security (RLS)
# The service role key is a secret - it bypasses RLS and must be kept private

# Your Supabase project URL
# Format: https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://example-project-id.supabase.co

# Your Supabase anonymous API key (safe to expose - protected by RLS)
# This key is marked NEXT_PUBLIC because it's used in the browser for authentication
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV4YW1wbGUiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYwMDAwMDAwMCwiZXhwIjoxOTAwMDAwMDB9.example_anon_key

# Your Supabase service role key (KEEP SECRET - server only!)
# This has full database access and bypasses Row Level Security (RLS)
# Never expose this to the browser
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV4YW1wbGUiLCJyb2xlIjoic2VydmljZV9yb2xlIiwiaWF0IjoxNjAwMDAwMDAwLCJleHAiOjE5MDAwMDAwMDB9.example_service_role_key

# ============================================================================
# Application Configuration
# ============================================================================

# Your application's public URL (used for redirects and CORS)
# Format: http://localhost:3000 (dev) or https://yourdomain.com (production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================================================
# AI Chatbot Configuration
# ============================================================================
# Powers the semantic search chatbot that answers questions about your site.
# Uses OpenAI for embeddings and chat completions via Vercel AI SDK.
# Content is indexed in Supabase using pgvector for similarity search.

# OpenAI API Key (required for chatbot)
# Get from: https://platform.openai.com/api-keys
# Used for: GPT-4o mini chat completions and text-embedding-3-small embeddings
OPENAI_API_KEY=sk-your-openai-api-key-here

# Vector Search Configuration (optional - defaults shown)
# VECTOR_SEARCH_SIMILARITY_THRESHOLD: Minimum similarity score (0-1) for results
# VECTOR_SEARCH_MAX_RESULTS: Maximum number of context chunks to include
VECTOR_SEARCH_SIMILARITY_THRESHOLD=0.7
VECTOR_SEARCH_MAX_RESULTS=5
