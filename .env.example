# Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# Never commit .env with real secrets - this file (.env.example) is safe to share
#
# Think of environment variables as the settings dial on your oven:
# Different settings for different purposes (development vs production)

# ============================================================================
# General Settings - Basic app configuration
# ============================================================================

# What mode are we running in? (development or production)
# Controls error messages, logging, and optimizations
NODE_ENV=development

# What's your app's public URL? (without trailing slash)
# Used in authentication redirects and other redirects
APP_URL=http://localhost:3000

# Name of your application (appears in browser tabs, emails, etc)
APP_NAME=Foundation Four

# ============================================================================
# Supabase Configuration - Your database and auth provider
# ============================================================================
# Supabase is the "filing cabinet" that permanently stores your data
# Get these values from: https://app.supabase.com
#
# To find your Supabase keys:
# 1. Go to your project settings (gear icon)
# 2. Click "API" in the left sidebar
# 3. Copy the Project URL and anon key

# Your Supabase project's public URL (looks like https://[project-id].supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous API key (safe to expose in browser - use RLS for security)
# This is marked NEXT_PUBLIC because the browser needs it for Supabase client
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Your Supabase service role key (KEEP SECRET - never expose to browser)
# This has elevated permissions - only use on the server/backend
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# Redis Configuration - Fast temporary storage and caching
# ============================================================================
# Redis is the "whiteboard" - fast but temporary storage
# In Docker, Redis URL is: redis://redis:6379 (redis is the container name)
# If using Redis Cloud: https://redis.com/try-free/

# Redis connection URL
# Format: redis://[username:password@]hostname:port
# Local Docker: redis://redis:6379
# Redis Cloud: redis://default:[password]@[host]:6379
REDIS_URL=redis://redis:6379

# ============================================================================
# Session & Security - Keeping users logged in securely
# ============================================================================
# These are like locks on your filing cabinet - they prove you're authorized

# Secret for encrypting session cookies (generate with: openssl rand -base64 32)
# Make this unique and never share it
SESSION_SECRET=your-super-secret-string-here-change-this-in-production

# How long sessions last before requiring re-login (in seconds)
# 30 days = 2592000 seconds
SESSION_MAX_AGE=2592000

# ============================================================================
# Stripe Configuration (Optional - only if you're accepting payments)
# ============================================================================
# Get from: https://dashboard.stripe.com/apikeys

# Public key (safe to expose in browser)
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_...

# Secret key (keep secret - server only)
STRIPE_SECRET_KEY=sk_test_...

# ============================================================================
# Development Tools (Optional - only needed during development)
# ============================================================================

# Enable debug logging (true or false)
DEBUG=false

# ============================================================================
# Docker-specific settings (automatically set by docker-compose.yml)
# ============================================================================
# These are usually set automatically when using Docker, but listed here for reference

# Database host (set to "redis" when using Docker)
REDIS_HOST=redis

# Database port (usually 6379)
REDIS_PORT=6379
