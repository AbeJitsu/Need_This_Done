'use client';

import { useCallback, useEffect, useState } from 'react';
import { AlertCircle, Check, Loader2, Mail, Reply, ShieldAlert, Users, X } from 'lucide-react';
import type { GrowthProfile, OutreachMessage, Prospect, ProspectingQueue } from '@/lib/prospecting-types';
import { MODEL_EVALUATION_DAILY_CAP_USD } from '@/lib/model-evaluation';

type Tab = 'morning' | 'midday' | 'evening';
const blank = { name: 'NeedThisDone growth profile', targetMarket: '', geography: '', businessSize: '', painSignals: '', exclusionRules: '', offer: '', senderName: '', senderEmail: '', dailyProspectCap: 10, dailySendCap: 10, workingHoursStart: '09:00', workingHoursEnd: '17:00', timezone: 'America/New_York', followUpDays: '3,7', modelRoute: 'evaluation-required', fallbackModel: '', perRunModelCap: 0.1, dailyModelCap: MODEL_EVALUATION_DAILY_CAP_USD, emergencyStop: false };
type ProfileForm = typeof blank;

function formFor(profile: GrowthProfile | null): ProfileForm {
  if (!profile) return { ...blank };
  return { ...blank, name: profile.name, targetMarket: profile.target_market, geography: profile.geography, businessSize: profile.business_size, painSignals: profile.pain_signals.join('\n'), exclusionRules: profile.exclusion_rules.join('\n'), offer: profile.offer, senderName: profile.sender_name, senderEmail: profile.sender_email, dailyProspectCap: profile.daily_prospect_cap, dailySendCap: profile.daily_send_cap, workingHoursStart: profile.working_hours_start.slice(0, 5), workingHoursEnd: profile.working_hours_end.slice(0, 5), timezone: profile.timezone, followUpDays: profile.follow_up_days.join(','), modelRoute: profile.model_route, fallbackModel: profile.fallback_model, perRunModelCap: Number(profile.per_run_model_cap), dailyModelCap: Number(profile.daily_model_cap), emergencyStop: profile.emergency_stop };
}

const inputClass = 'mt-1 min-h-11 w-full rounded-xl border border-[#183229]/20 bg-white px-3';

export default function ProspectingDashboard() {
  const [queue, setQueue] = useState<ProspectingQueue | null>(null);
  const [form, setForm] = useState<ProfileForm>({ ...blank });
  const [tab, setTab] = useState<Tab>('morning');
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [notice, setNotice] = useState<string | null>(null);
  const [setupOpen, setSetupOpen] = useState(false);
  const [drafting, setDrafting] = useState<string | null>(null);
  const [editing, setEditing] = useState<Record<string, { subject: string; body: string }>>({});

  const load = useCallback(async () => {
    setLoading(true); setError(null);
    try {
      const response = await fetch('/api/prospecting/queue', { cache: 'no-store' });
      const data = await response.json();
      if (!response.ok) throw new Error(data.error || 'Prospecting queue could not be loaded.');
      setQueue(data); setForm(formFor(data.profile)); if (!data.profile) setSetupOpen(true);
    } catch (loadError) { setError(loadError instanceof Error ? loadError.message : 'Prospecting queue could not be loaded.'); }
    finally { setLoading(false); }
  }, []);
  useEffect(() => { void load(); }, [load]);

  const setField = (field: keyof ProfileForm, value: string | number | boolean) => setForm((current) => ({ ...current, [field]: value }));
  const saveProfile = async (event: React.FormEvent) => {
    event.preventDefault(); setSaving(true); setError(null);
    const payload = { ...form, painSignals: form.painSignals.split('\n').map((item) => item.trim()).filter(Boolean), exclusionRules: form.exclusionRules.split('\n').map((item) => item.trim()).filter(Boolean), followUpDays: form.followUpDays.split(',').map(Number).filter(Number.isInteger) };
    try { const response = await fetch('/api/prospecting/profile', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Profile could not be saved.'); setSetupOpen(false); setNotice('Growth profile saved.'); await load(); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : 'Profile could not be saved.'); } finally { setSaving(false); }
  };
  const emergencyStop = async () => { const active = !queue?.profile?.emergency_stop; const response = await fetch('/api/prospecting/emergency-stop', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ active }) }); if (!response.ok) { setError('Emergency-stop state could not be changed.'); return; } setNotice(active ? 'Emergency stop is active.' : 'Emergency stop cleared.'); await load(); };
  const createDraft = async (prospect: Prospect) => { setDrafting(prospect.id); const response = await fetch('/api/prospecting/messages', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ prospectId: prospect.id, subject: `${prospect.company_name}: one practical growth idea`, body: `Hi ${prospect.contact_name || 'there'},\n\nI noticed ${prospect.icp_match_reason.toLowerCase()} I have one practical idea that may help: ${queue?.profile?.offer || 'a focused improvement plan'}.\n\nWould a short conversation be useful?\n\nBest,\n${queue?.profile?.sender_name || 'NeedThisDone'}`, evidence: (prospect.prospect_sources || []).flatMap((source) => source.evidence.map(String)).slice(0, 5) }) }); if (!response.ok) { const data = await response.json(); setError(data.error || 'Draft could not be created.'); } else { setNotice('Draft prepared for midday review.'); await load(); } setDrafting(null); };
  const decide = async (message: OutreachMessage, decision: 'approve' | 'reject' | 'defer') => { const value = editing[message.id] || { subject: message.subject, body: message.body }; const response = await fetch(`/api/prospecting/messages/${message.id}/decision`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ decision, ...value, nextActionAt: decision === 'defer' ? new Date(Date.now() + 3 * 86_400_000).toISOString() : undefined }) }); if (!response.ok) { const data = await response.json(); setError(data.error || 'Message decision failed.'); return; } setNotice(`Message ${decision}d.`); await load(); };
  const sendApproved = async (message: OutreachMessage) => { const response = await fetch('/api/prospecting/sender/send', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ messageId: message.id }) }); const data = await response.json(); if (!response.ok) { setError(data.error || 'Approved message could not be sent.'); return; } setNotice(data.duplicate ? 'The approved message was already sent.' : 'Approved message sent through the configured sender.'); await load(); };

  if (loading) return <div className="grid min-h-[65vh] place-items-center bg-[#f7f4ed]"><p className="flex items-center gap-3 font-bold"><Loader2 className="h-5 w-5 animate-spin" />Loading prospecting workspace…</p></div>;
  return <div className="min-h-screen bg-[#f7f4ed] px-5 py-8 text-[#183229] sm:px-8"><div className="mx-auto max-w-7xl">
    <header className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#126b4e]">NeedThisDone · supervised growth</p><h1 className="mt-2 text-4xl font-black">Prospecting &amp; outreach</h1><p className="mt-2 max-w-2xl text-[#50675e]">Discover public prospects, review evidence-backed drafts, and measure replies from three short check-ins.</p></div><div className="flex gap-2"><button onClick={() => setSetupOpen((value) => !value)} className="min-h-11 rounded-full border border-[#183229]/20 bg-white px-4 font-bold">{setupOpen ? 'Close setup' : 'Growth profile'}</button>{queue?.profile && <button onClick={() => void emergencyStop()} className={`inline-flex min-h-11 items-center gap-2 rounded-full px-4 font-bold ${queue.profile.emergency_stop ? 'bg-[#183229] text-white' : 'bg-red-100 text-red-800'}`}><ShieldAlert className="h-4 w-4" />{queue.profile.emergency_stop ? 'Clear stop' : 'Emergency stop'}</button>}</div></header>
    {error && <div role="alert" className="mt-6 flex gap-2 rounded-xl bg-red-50 p-4 text-red-800"><AlertCircle className="h-5 w-5 shrink-0" />{error}</div>}{notice && <div className="mt-6 rounded-xl bg-emerald-50 p-4 text-emerald-900">{notice}</div>}
    {setupOpen && <ProfileForm form={form} saving={saving} onChange={setField} onSubmit={saveProfile} />}
    {!queue?.profile ? <Empty title="Start with your market profile" description="Configure the profile above before discovery can run." icon={<Users className="mx-auto h-8 w-8 text-[#126b4e]" />} /> : <>
      <Stats queue={queue} />
      <nav aria-label="Prospecting check-ins" className="mt-8 grid gap-2 sm:grid-cols-3">{(['morning', 'midday', 'evening'] as Tab[]).map((item) => <button key={item} onClick={() => setTab(item)} className={`min-h-14 rounded-xl px-4 text-left font-bold ${tab === item ? 'bg-[#18372e] text-white' : 'border border-[#183229]/15 bg-white'}`}>{item === 'morning' ? 'Morning · discover' : item === 'midday' ? 'Midday · review drafts' : 'Evening · replies & next actions'}</button>)}</nav>
      {tab === 'morning' && <Morning queue={queue} onDraft={(prospect) => void createDraft(prospect)} drafting={drafting} />}
      {tab === 'midday' && <Midday queue={queue} editing={editing} setEditing={setEditing} onDecision={(message, decision) => void decide(message, decision)} onSend={(message) => void sendApproved(message)} />}
      {tab === 'evening' && <Evening queue={queue} />}
    </>}
  </div></div>;
}

function Empty({ title, description, icon }: { title: string; description: string; icon: React.ReactNode }) { return <div className="mt-8 rounded-3xl border border-dashed border-[#183229]/25 bg-white p-12 text-center">{icon}<h2 className="mt-4 text-2xl font-black">{title}</h2><p className="mt-2 text-[#50675e]">{description}</p></div>; }
function Stats({ queue }: { queue: ProspectingQueue }) { const values: Array<[string, string | number]> = [['Discovered', queue.stats.discoveredToday], ['Drafts', queue.stats.pendingDrafts], ['Approved', queue.stats.approvedToday], ['Sent', queue.stats.sentToday], ['Replies', queue.stats.replies], ['Bounces', queue.stats.bounces], ['Model spend', `$${queue.stats.modelSpendToday.toFixed(2)}`]]; return <section className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-7">{values.map(([label, value]) => <article key={label} className="rounded-2xl border border-[#183229]/15 bg-white p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#126b4e]">{label}</p><p className="mt-2 text-2xl font-black">{value}</p></article>)}</section>; }
function ProfileForm({ form, saving, onChange, onSubmit }: { form: ProfileForm; saving: boolean; onChange: (field: keyof ProfileForm, value: string | number | boolean) => void; onSubmit: (event: React.FormEvent) => void }) { const fields: Array<[keyof ProfileForm, string, boolean]> = [['name', 'Profile name', false], ['targetMarket', 'Target market', true], ['geography', 'Geography', true], ['businessSize', 'Business size/type', false], ['offer', 'Offer', true], ['senderName', 'Sender name', true], ['senderEmail', 'Approved sender email', true], ['timezone', 'Timezone', true]]; return <form onSubmit={onSubmit} className="mt-8 rounded-3xl border border-[#183229]/15 bg-white p-6 sm:p-8"><div className="flex items-center justify-between"><div><h2 className="text-2xl font-black">Configure once</h2><p className="mt-1 text-sm text-[#50675e]">Limits apply to discovery, drafting, and any future approved sender.</p><p className="mt-2 text-sm text-[#50675e]">Live model work remains off until the fixed evaluation clears a route. The maximum evaluation budget is ${MODEL_EVALUATION_DAILY_CAP_USD.toFixed(2)} per day.</p></div><Check className="h-6 w-6 text-[#126b4e]" /></div><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{fields.map(([field, label, required]) => <label key={field} className="font-semibold">{label}<input required={required} value={String(form[field])} onChange={(event) => onChange(field, event.target.value)} className={inputClass} /></label>)}<label className="font-semibold">Daily prospect cap<input type="number" min="1" max="100" value={form.dailyProspectCap} onChange={(event) => onChange('dailyProspectCap', Number(event.target.value))} className={inputClass} /></label><label className="font-semibold">Daily approved-send cap<input type="number" min="1" max="100" value={form.dailySendCap} onChange={(event) => onChange('dailySendCap', Number(event.target.value))} className={inputClass} /></label><label className="font-semibold">Follow-up days<input value={form.followUpDays} onChange={(event) => onChange('followUpDays', event.target.value)} className={inputClass} placeholder="3,7" /></label><label className="font-semibold">Working hours start<input type="time" value={form.workingHoursStart} onChange={(event) => onChange('workingHoursStart', event.target.value)} className={inputClass} /></label><label className="font-semibold">Working hours end<input type="time" value={form.workingHoursEnd} onChange={(event) => onChange('workingHoursEnd', event.target.value)} className={inputClass} /></label><label className="font-semibold sm:col-span-2 lg:col-span-3">Pain signals, one per line<textarea value={form.painSignals} onChange={(event) => onChange('painSignals', event.target.value)} className={`${inputClass} min-h-20 p-3`} /></label><label className="font-semibold sm:col-span-2 lg:col-span-3">Exclusion rules, one per line<textarea value={form.exclusionRules} onChange={(event) => onChange('exclusionRules', event.target.value)} className={`${inputClass} min-h-20 p-3`} /></label></div><button disabled={saving} className="mt-6 min-h-11 rounded-full bg-[#126b4e] px-5 font-bold text-white">{saving ? 'Saving…' : 'Save growth profile'}</button></form>; }
function Morning({ queue, onDraft, drafting }: { queue: ProspectingQueue; onDraft: (prospect: Prospect) => void; drafting: string | null }) { return <section className="mt-6 grid gap-5 lg:grid-cols-[1fr_320px]"><div className="space-y-4">{queue.prospects.length === 0 ? <Empty title="No prospects discovered yet" description="The public-web worker can fill this queue when discovery is scheduled. No prospect is added from the production-facing workspace." icon={<Users className="mx-auto h-8 w-8 text-[#126b4e]" />} /> : queue.prospects.map((prospect) => <article key={prospect.id} className="rounded-3xl border border-[#183229]/15 bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-[#126b4e]">ICP match {prospect.icp_match_score}% · {prospect.outreach_status}</p><h2 className="mt-2 text-2xl font-black">{prospect.company_name}</h2><p className="mt-1 text-[#50675e]">{prospect.contact_name || 'Contact not identified'} {prospect.contact_title ? `· ${prospect.contact_title}` : ''} {prospect.email ? `· ${prospect.email}` : ''}</p></div>{prospect.email && prospect.suppression_status === 'clear' && <button disabled={drafting === prospect.id} onClick={() => onDraft(prospect)} className="inline-flex min-h-11 items-center gap-2 rounded-full bg-[#126b4e] px-4 font-bold text-white"><Mail className="h-4 w-4" />{drafting === prospect.id ? 'Preparing…' : 'Prepare draft'}</button>}</div><p className="mt-4 leading-7 text-[#50675e]">{prospect.icp_match_reason}</p><div className="mt-4 rounded-2xl bg-[#f7f4ed] p-4"><p className="text-sm font-bold">Public evidence</p>{prospect.prospect_sources?.map((source) => <div key={source.id} className="mt-2 text-sm"><a className="text-[#126b4e] underline" href={source.source_url} target="_blank" rel="noreferrer">{source.source_url}</a><p className="mt-1 text-[#50675e]">{source.evidence.map(String).join(' · ')}</p></div>)}</div></article>)}</div><aside className="rounded-3xl bg-[#d9b96e]/35 p-6"><h2 className="text-xl font-black">Morning guardrails</h2><ul className="mt-4 space-y-3 text-sm leading-6"><li>Discover up to {queue.profile?.daily_prospect_cap} prospects.</li><li>Only public HTTPS sources are accepted.</li><li>Duplicates and suppressed addresses are skipped.</li><li>Discovery and drafting never send.</li></ul></aside></section>; }
function Midday({ queue, editing, setEditing, onDecision, onSend }: { queue: ProspectingQueue; editing: Record<string, { subject: string; body: string }>; setEditing: React.Dispatch<React.SetStateAction<Record<string, { subject: string; body: string }>>>; onDecision: (message: OutreachMessage, decision: 'approve' | 'reject' | 'defer') => void; onSend: (message: OutreachMessage) => void }) { const messages = queue.messages.filter((message) => message.approval_status === 'pending' || message.approval_status === 'deferred' || message.approval_status === 'approved'); if (!messages.length) return <Empty title="Draft review is clear" description="Personalized messages wait here until you approve, reject, edit, or defer each one." icon={<Mail className="mx-auto h-8 w-8 text-[#126b4e]" />} />; return <section className="mt-6 space-y-5">{messages.map((message) => { const value = editing[message.id] || { subject: message.subject, body: message.body }; const approved = message.approval_status === 'approved'; return <article key={message.id} className="rounded-3xl border border-[#183229]/15 bg-white p-6"><p className="text-xs font-bold uppercase tracking-widest text-[#126b4e]">To {message.recipient_email} · {queue.prospects.find((item) => item.id === message.prospect_id)?.company_name || 'prospect'}</p><h2 className="mt-2 text-2xl font-black">{approved ? 'Approved and ready to send' : 'Message preview'}</h2><label className="mt-5 block font-semibold">Subject<input disabled={approved} value={value.subject} onChange={(event) => setEditing((current) => ({ ...current, [message.id]: { ...value, subject: event.target.value } }))} className={inputClass} /></label><label className="mt-4 block font-semibold">Body<textarea disabled={approved} value={value.body} onChange={(event) => setEditing((current) => ({ ...current, [message.id]: { ...value, body: event.target.value } }))} className={`${inputClass} min-h-48 p-3`} /></label><p className="mt-4 rounded-xl bg-[#f7f4ed] p-3 text-sm text-[#50675e]">{approved ? 'This exact approved record is the only message the sender can execute.' : 'Sending is blocked until this exact record is approved.'}</p><div className="mt-4 flex flex-wrap gap-2">{approved ? <button onClick={() => onSend(message)} className="min-h-11 rounded-full bg-[#126b4e] px-4 font-bold text-white">Send approved message</button> : <><button onClick={() => onDecision(message, 'approve')} className="min-h-11 rounded-full bg-[#126b4e] px-4 font-bold text-white">Approve</button><button onClick={() => onDecision(message, 'defer')} className="min-h-11 rounded-full border border-[#183229]/20 px-4 font-bold">Defer</button><button onClick={() => onDecision(message, 'reject')} className="inline-flex min-h-11 items-center gap-2 rounded-full border border-red-900/20 px-4 font-bold text-red-800"><X className="h-4 w-4" />Reject</button></>}</div></article>; })}</section>; }
function Evening({ queue }: { queue: ProspectingQueue }) { return <section className="mt-6 grid gap-5 lg:grid-cols-[1fr_320px]"><div className="space-y-3">{queue.outcomes.length === 0 ? <Empty title="No outcomes yet" description="Delivery, replies, bounces, unsubscribe events, and next actions will appear here." icon={<Reply className="mx-auto h-8 w-8 text-[#126b4e]" />} /> : queue.outcomes.map((outcome) => <article key={outcome.id} className="rounded-2xl border border-[#183229]/15 bg-white p-5"><p className="text-xs font-bold uppercase tracking-widest text-[#126b4e]">{outcome.outcome_type}</p><p className="mt-2">{outcome.notes || 'No notes.'}</p></article>)}</div><aside className="rounded-3xl border border-[#183229]/15 bg-white p-6"><h2 className="text-xl font-black">Safety status</h2><p className="mt-3 text-sm leading-6 text-[#50675e]">The sender boundary is provider-neutral. Provider events are idempotent by event ID and bounces/unsubscribes create suppression records.</p><p className="mt-4 font-bold">{queue.stats.replies} replies · {queue.stats.bounces} bounces · {queue.stats.unsubscribes} unsubscribes</p></aside></section>; }
