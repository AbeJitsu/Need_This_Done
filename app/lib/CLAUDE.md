# Claude compatibility bridge

The canonical guidance for this subtree is `app/lib/AGENTS.md`. Read the root
`AGENTS.md` and that file before changing application-library code. Do not
duplicate subsystem rules here.
