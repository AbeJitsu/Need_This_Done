# Environment Configuration Template - Next.js Application
# Copy this file to .env.local and fill in your actual values
# Never commit .env with real secrets - this file (.env.example) is safe to share
#
# This file is for Next.js application runtime configuration.
# For Docker Compose and infrastructure variables, see .env.example in the root directory.

# ============================================================================
# Browser-Accessible Variables
# ============================================================================
# These variables are safe to expose in the browser (via NEXT_PUBLIC_ prefix).
# They are protected by Supabase Row Level Security (RLS) policies.
# Never put secrets here even if they have NEXT_PUBLIC_ prefix!

# Your Supabase project URL
# Get from: https://app.supabase.com → Project Settings → API
# Format: https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://example-project-id.supabase.co

# Your Supabase anonymous API key
# Get from: https://app.supabase.com → Project Settings → API
# This is safe to expose - it's protected by Row Level Security (RLS) in the database
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV4YW1wbGUiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYwMDAwMDAwMCwiZXhwIjoxOTAwMDAwMDB9.example_anon_key

# Your application's public URL
# Used for email confirmation links, password reset URLs, and redirects after authentication
# Format: http://localhost:3000 (dev) or https://yourdomain.com (production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================================================
# Server-Only Variables (KEEP THESE SECRET)
# ============================================================================
# These variables are only available on the server. They are NOT exposed to the browser.
# Keep these in .env.local and NEVER commit them to version control!

# Your Supabase service role key (KEEP SECRET!)
# Get from: https://app.supabase.com → Project Settings → API
# This has full database access and bypasses Row Level Security (RLS)
# Only use on the server for admin operations. NEVER expose to the browser!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImV4YW1wbGUiLCJyb2xlIjoic2VydmljZV9yb2xlIiwiaWF0IjoxNjAwMDAwMDAwLCJleHAiOjE5MDAwMDAwMDB9.example_service_role_key

# Redis connection URL
# Used for caching and session storage
# Docker: redis://redis:6379 (service name resolution within Docker network)
# Local: redis://localhost:6379 (if running Redis separately)
REDIS_URL=redis://redis:6379

# ============================================================================
# Application Configuration
# ============================================================================

# Node environment
# development = Hot reload, detailed logging, debug tools enabled
# production = Optimized build, minimal logging
# test = Test environment with special configurations
NODE_ENV=development

# Medusa ecommerce backend URL
# Points to the Medusa service for product catalog, cart, checkout
# Docker: http://medusa:9000 (service name resolution within Docker network)
# Local: http://localhost:9000 (if running Medusa separately)
MEDUSA_BACKEND_URL=http://medusa:9000
