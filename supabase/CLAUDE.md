# Claude compatibility bridge

The canonical guidance for this subtree is `supabase/AGENTS.md`. Read the root
`AGENTS.md` and that file before changing migrations, RLS, or Storage. Do not
duplicate database rules here.
